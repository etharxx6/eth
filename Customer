customer_data = {}  # Dictionary to store customer data

def add_customer():
    customer_id = input("Enter customer ID: ")
    name = input("Enter customer name: ")
    email = input("Enter customer email: ")
    phone = input("Enter customer phone number: ")

     # Create an issue to store customer data
    issue_title = "Customer Data - {}".format(customer_data["customer_id"])
    issue_body = """
    Customer ID: {}
    Name: {}
    Email: {}
    Phone: {}
    """.format(customer_data["customer_id"], customer_data["name"], customer_data["email"], customer_data["phone"])
 # Create the issue
    issue = repo.create_issue(title=issue_title, body=issue_body)
    print("Customer data stored successfully as an issue.")
